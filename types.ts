export interface Objective {
  id: number;
  title: string;
  description: string;
}

export interface Benefit {
  id: number;
  text: string;
}

export interface ScheduleItem {
  day: string;
  title: string;
  desc: string;
}
